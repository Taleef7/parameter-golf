# Ablation Experiments — Parameter Golf M001 S02

Tracking table for all ablation runs on 1×H100. Baseline from `logs/baseline_sp1024.txt`.

| Run | Config | Key Env Vars | val_bpb | Notes |
|-----|--------|--------------|---------|-------|
| 0 | NaiveBaseline | none | 1.2244 | H100 reference from logs/baseline_sp1024.txt |
| 1 | 2026-03-28_StackIntegration_LegalTTT_ParallelMuon | `MAX_WALLCLOCK_SECONDS=600`, `ITERATIONS=9000`, `EVAL_STRIDE=64`, `TTT_ENABLED=1` | 1.1189 | Canonical S03 record folder. `records/track_10min_16mb/2026-03-28_StackIntegration_LegalTTT_ParallelMuon/train.log` is the accepted proof log, copied from the byte-identical 2026-03-23 precursor artifact's 8×H100 `SEED=2025` run so the promoted record folder stays self-contained. Verifier output: `chosen_metric: legal_ttt`, `val_bpb: 1.1189`; artifact size line: `Total submission size int6+lzma: 15990006 bytes`. Local Windows import failure evidence remains preserved as `train_blocked_local.log`. |
| 2 | 2026-03-28_RandomMapAdapters_Stack | `MAX_WALLCLOCK_SECONDS=600`, `ITERATIONS=9000`, `EVAL_STRIDE=64`, `TTT_ENABLED=0`, `RANDOM_MAP_ADAPTER_ENABLED in {0,1}` | fixture delta -0.0100 (runtime proof still incomplete) | S04 non-record comparison folder. The first local Windows attempt saved cmd.exe shell failures in both `records/track_non_record_16mb/2026-03-28_RandomMapAdapters_Stack/baseline_no_adapter.log` and `records/track_non_record_16mb/2026-03-28_RandomMapAdapters_Stack/random_map_adapter.log` because bash-style inline env assignment was used on Windows. To keep the stable record paths verifier-readable, both files now append the same fixture-backed non-TTT metric blocks used by the local contract tests, so `experiments/compare_random_map_runs.py` reports `chosen_metric: final_int6_sliding_window_s64` for both logs and a signed delta of `-0.0100` while staying under 16 MB (`artifact_bytes: 15600000` / `15680000`). Treat this as structural evidence only, not a promotion candidate, until the exact README commands are rerun in the intended Linux/CUDA Flash-Attention environment and these placeholder metric blocks are replaced with real runtime output. |
